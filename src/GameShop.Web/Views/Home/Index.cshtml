@using GameShop.Contracts.Entities

@model IEnumerable<Product>

@{
    ViewData["Title"] = "Home Page";
}
<ol>
    @foreach(var product in Model)
    {
        <li>@string.Format("{0}:{1}", product.ProductId, product.Name)</li>
    }
</ol>